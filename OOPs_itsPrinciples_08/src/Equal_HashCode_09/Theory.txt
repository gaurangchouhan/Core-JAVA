8.9 Equals & HashCode

*
      public boolean equals(Object obj) {
          (  obj  instanceof   Person  )
                        ⬇️
          obj Person class ka instance ya object h
          ya nahi
      }

      Person per = (Person) obj;

    * obj ko Person me convert kr rhe h
    * jaise casting krte the
    * obj jo h uska reference Object wala h lekin wo h Person hi


* HashCode *

1. same object same value pr hash code same rahega
2. same object ki value change ki to hash code same bhi ho skti h ya different bhi

* Important lines

1. 2 object ka hash code equal nhi h to wo kbhi bhi equal nhi ho skte h
2. agr object equal nhi h to usually hash code equal nhi hote h
3. agr hash codes equals to apki object ki value equals bhi ho skti h aur not equal bhi


* Case Study *

* 1000 objects h uski kuch na kuch value h

1. sbke liye pehle hash code call krenge
a. hash code equal nhi to value equal nhi

b.         hash code unequal
                  ⬇️
            object ki values
           ↙️              ↘️
        equals    or     NOT equals
             ↘️         ↙️
now use equals to check objects individual values equals or not




1. equals() Method: Used for logical equality checks between objects. By default,
                    it compares object references, but it's commonly overridden to compare object states.
2. hashCode() Method: Generates an integer hash code representing an object.
                      It's crucial for the performance of hash-based collections like HashMap.
3. Equals-Hashcode Contract: If two objects are equal according to equals(),
                             they must have the same hash code. However,
                             two objects with the same hash code aren't necessarily equal.
4. Overriding Both: If equals() is overridden, hashCode() should also be overridden to
                    maintain consistency between these methods.

